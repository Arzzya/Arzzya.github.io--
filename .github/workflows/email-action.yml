name: Email in Action Step

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: 23f2001189@ds.study.iitm.ac.in
      run: |
        echo "This step contains my email address in the name"
        echo "Current directory: $(pwd)"
        echo "Workflow triggered by: ${{ github.actor }}"
        
    - name: Normal step
      run: echo "This is a regular step"
      
    - name: Final step with timestamp
      run: echo "Workflow completed at $(date)"
